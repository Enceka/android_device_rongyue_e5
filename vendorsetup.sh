# Lunch combos are registered through COMMON_LUNCH_CHOICES in
# AndroidProducts.mk (lineage_e5-user/-userdebug/-eng).  The legacy
# `add_lunch_combo` call was dropped in Android 10, so it is intentionally
# omitted here.
